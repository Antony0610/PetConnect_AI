name: petconnect_ai
description: "PetConnect AI — Integrated AI + IoT Smart Pet Platform & Collar Ecosystem (5 Volumes)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0

dependency_overrides:
  path_provider_android: 2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
